server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DraganovTs/grow-me-config.git




#        native:
#          search-locations: "classpath:/config"